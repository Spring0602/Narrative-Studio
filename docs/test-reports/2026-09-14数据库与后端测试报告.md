# 叙事工坊：2026-09-10 数据库与后端测试报告

负责人：数据库设计与测试  
测试数据库：`narrative_studio_design_test`  
测试范围：数据库结构、后端接口、随机业务链路、发布快照、剧情图问题检测

## 1. 测试结论

本次已完成数据库和后端核心功能的测试。数据库结构满足当前设计，后端服务可正常启动，核心业务链路、权限控制、发布版本固定和剧情图问题检测均已验证通过。

所有自动化测试数据只写入测试库，不修改数据库表结构，也不修改前端或后端源代码。

## 2. 数据库结构核验

测试库共有 **20 张业务表**：17 张基础表，以及 `account_action_token`、`story_choice_draft`、`story_release` 三张扩展表。

- `sys_user`：`email`、`email_verified_at`、`token_version` 已存在；邮箱有唯一约束。
- `test_feedback.step_id`：已关联 `playtest_step.id`。
- `playtest_step`：已保存 `knowledge_before`、`knowledge_after` JSON 快照。
- `playtest_session.release_id`：已关联 `story_release.id`。

## 3. 已通过的测试

| 范围 | 结果 |
|---|---|
| 后端自动测试 | 141 通过，0 失败，0 错误，1 跳过 |
| 真实 MySQL 冒烟 | 1 通过，0 失败，0 错误，0 跳过 |
| 健康检查 | 服务状态 `UP` |
| 登录与权限 | 注册、正确/错误登录、未登录 401、非成员 403 均通过 |
| 剧情试玩 | 变量、条件、效果、步骤快照、反馈关联均通过 |
| 随机业务链路 | 基础随机测试 5/5 通过；条件不可用与跨账号权限已加测 |
| 发布版本固定 | 发布后编辑原稿不影响发布版试玩，已通过 |
| 剧情图问题检测 | 孤立和不可达节点的检测、修复后自动关闭，3/3 通过 |

## 4. 严格验收结论

代码已进入联调阶段，但“代码存在”不等于“真实环境已验收”。9 月 11 日后新增的前端页面、导入导出、结局覆盖和可选 AI 功能，尚未完成真实浏览器与真实 MySQL 的最终验收。

`/projects/1?preview=1` 是原型预览模式，仅用于界面查看，不能作为真实数据验收证据。通常前端地址为 `http://localhost:5173/projects/1?preview=1`，以 Vite 启动时显示的端口为准。

## 5. 必须补做的 P0 验收

- 真实浏览器：注册、登录、退出、改密、找回密码；项目与成员角色权限；世界观、角色、关系、知识；剧情画布和规则；试玩、反馈、发布、结局覆盖、结构检测和账户页面。
- 真实 MySQL：使用 `narrative_studio_design_test` 执行最新冒烟测试；补完整 SQL 多节点双结局演示数据；保留 Navicat 的表、外键、索引和步骤截图。
- 边界与可靠性：500 节点、1000 选项、200 变量容量；并发推进同一会话；归档项目权限；无效 JSON 导入回滚；被引用对象删除拒绝；分页多页完整加载。

## 6. 明确风险

1. 最新前端尚未做真实页面点击、截图和视觉验收。
2. SMTP 目前只有测试替身，真实邮件投递未验收。
3. AI 默认关闭，未配置真实模型时不能标记为通过。
4. 前端生产构建通过，但仍需验证首屏加载和交互。
5. 真实 MySQL、并发负载、备份恢复和新机启动缺少最终证据。
6. JSON 导入导出、结局覆盖和新增 P1 页面需做真实前端联调；接口封装存在不等于页面完成。

## 7. C 的后续任务

- [x] 核验 20 表、关键字段、外键和索引。
- [x] 完成核心接口、随机链路、发布冻结和结构检测测试。
- [ ] 同步最新 GitHub 前端到本地并完成前端构建。
- [ ] 完成浏览器端到端用例并保存截图。
- [ ] 补齐完整 SQL 演示故事与说明。
- [ ] 执行最新真实 MySQL 冒烟测试。
- [ ] 明确 SMTP / AI 是否纳入答辩；未配真实服务时标记为未验收。
- [ ] 执行备份恢复和新机启动演练。
- [ ] 汇总数据库说明、测试报告、截图和答辩演示步骤。
